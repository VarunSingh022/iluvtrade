"""Application-level strategy identity and versioning.

The engine-level question ("what code runs?") is answered by
:mod:`iluvtrade.alphalab_bridge.strategies`. This package answers the product
questions: who owns a strategy, which revisions exist, which of them are frozen,
and which one a given run used.
"""
